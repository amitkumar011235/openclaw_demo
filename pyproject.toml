[project]
name = "openclaw-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain",
    "langchain-openai",
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "streamlit",
    "websockets",
]

